{
  "version": 1,
  "databases": [
    {
      "database": "sqlitexmtest",
      "isDefault": true
    },
    {
      "database": "logDb",
      "isDefault": false
    } 
  ],

  "_comment": "Do not include any comments in your SQL. For example, INSERT INTO user (name) /*comment */ VALUES('Bob');, or SELECT * FROM user WHERE name = 'Bob'; -- comment",

  "trigger": [
    {
      "Database": "sqlitexmtest",
      "Table Name": "user",
      "Statement": "CREATE TRIGGER updateCustomer AFTER INSERT ON user BEGIN INSERT INTO customer (name, address) VALUES (new.name, new.address); END;"
    }
  ],

  "insert": [
    {
      "Statement Name": "insertUser",
      "Table Name": "user",
      "Statement": "INSERT INTO user (intt, longg, floatt, doublee, decimall, booll, address, name, datetimee) VALUES(@intt, @longg, @floatt, @doublee, @decimall, @booll, @address, @name, @datetimee)"
    }
  ],

  "select": [
    {
      "Statement Name": "getUser",
      "Table Name": "user",
      "Statement": "SELECT * FROM user WHERE doublee = @p0 LIMIT 50"
    },
    {
      "Statement Name": "getUserNameValue",
      "Table Name": "user",
      "Statement": "SELECT * FROM user WHERE doublee = @doublee LIMIT 50"
    },
    {
      "Statement Name": "getCustomer",
      "Table Name": "customer",
      "Statement": "SELECT * FROM customer LIMIT 50"
    }
  ],

  "update": [
    {
      "Statement Name": "updateLastQuestionLocation",
      "Table Name": "user",
      "Statement": "UPDATE lastQuestionLocation SET topicQuestionId=@p0 WHERE memberId = @p1 AND topicID = @p2"
    }
  ],

  "delete": [
    {
      "Statement Name": "deleteUser",
      "Table Name": "user",
      "Statement": "DELETE FROM user WHERE doublee = @p0"
    }
  ]
}